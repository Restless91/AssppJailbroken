{
  "name": "asspp-private-platform",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "for file in *.mjs public/*.js; do node --check \"$file\" || exit 1; done",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22.5"
  }
}
